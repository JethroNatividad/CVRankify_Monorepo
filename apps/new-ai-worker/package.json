{
  "name": "new-ai-worker",
  "private": true,
  "scripts": {
    "setup": "python -m venv env && source env/bin/activate && pip install -r requirements.txt",
    "dev": "env/bin/python -u main.py"
  }
}
